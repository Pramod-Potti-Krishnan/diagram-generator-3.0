# ==================== DIAGRAM GENERATOR v3.0 CONFIGURATION ====================

# ==================== LLM CONFIGURATION (GEMINI via VERTEX AI) ====================
# RECOMMENDED: Use Vertex AI with GCP Service Accounts for production

# GCP Project ID (REQUIRED for Vertex AI)
GCP_PROJECT_ID=your-project-id-here

# Vertex AI Configuration
GEMINI_LOCATION=us-central1

# Diagram-specific LLM Model (REQUIRED)
# Options: gemini-2.0-flash-exp, gemini-1.5-flash, gemini-1.5-pro, gemini-2.5-flash
LLM_DIAGRAM=gemini-2.0-flash-exp

# Optional: Separate model for Mermaid diagrams (defaults to LLM_DIAGRAM)
# LLM_DIAGRAM_MERMAID=gemini-2.0-flash-exp

# ==================== AUTHENTICATION ====================

# Two options for GCP authentication:

# OPTION 1: Direct JSON Paste (Recommended for Railway/Cloud Deployments)
# Paste your entire service account JSON key as a single-line string
# GCP_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key":"..."}

# OPTION 2: File Path (Local Development)
# Point to service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
# Or use Application Default Credentials: gcloud auth application-default login

# ==================== LEGACY API KEY (BACKWARD COMPATIBILITY) ====================
# Only used if GCP_PROJECT_ID is not set
# GOOGLE_API_KEY=your-api-key-here

# ==================== SUPABASE CONFIGURATION ====================
# REQUIRED for diagram storage

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_BUCKET=diagram-charts

# ==================== REST API CONFIGURATION ====================

API_PORT=8080
JOB_CLEANUP_HOURS=1

# ==================== REDIS CONFIGURATION (OPTIONAL) ====================

# For caching - optional but recommended for production
# REDIS_URL=redis://localhost:6379

# ==================== APPLICATION CONFIGURATION ====================

APP_ENV=development
LOG_LEVEL=INFO
DEBUG=false

# Optional API Authentication
# API_KEY=

# CORS Configuration
CORS_ORIGINS=*

# ==================== NOTES ====================

# 1. Copy this file to .env and fill in your values
# 2. Never commit .env to version control
# 3. For Vertex AI (RECOMMENDED):
#    - Set GCP_PROJECT_ID, GEMINI_LOCATION, and LLM_DIAGRAM
#    - Use either GCP_CREDENTIALS_JSON or GOOGLE_APPLICATION_CREDENTIALS for auth
# 4. For Legacy API Key:
#    - Only set GOOGLE_API_KEY (will be used if GCP_PROJECT_ID is not set)
# 5. ADC must be configured for local dev: gcloud auth application-default login
